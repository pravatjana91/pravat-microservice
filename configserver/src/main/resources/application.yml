server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
     server:
      git:
       uri: https://github.com/pravatjana91/config-server
       clone-on-start: true
       force-pull: true
       
#http://localhost:9296/guestservices/default